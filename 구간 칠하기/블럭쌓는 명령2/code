n, k = map(int, input().split())

# Please write your code here.
blocks = [0] * (n + 1)
for _ in range(k):
    A, B = map(int, input().split()) 
    for i in range(A, B + 1):  
        blocks[i] += 1

print(max(blocks))
