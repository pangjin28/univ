N = input()

# Please write your code here.
num = 0
leng = len(N)

for i in range(leng):
    num += int(N[i]) * (2 ** (leng -i -1))

num = num * 17
c = []

while True:
    if num <2:
        c.append(num)
        break
    c.append(num % 2)
    num //= 2

for i in c[::-1]:
    print(i,end="")
